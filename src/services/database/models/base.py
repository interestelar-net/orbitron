# External imports:
from sqlalchemy.orm import DeclarativeBase

# Internal imports:

# Code:
class BaseModel(DeclarativeBase):
    """
    This class serves as a base model for all database models in the application. It can be extended by other models to inherit common functionality and attributes.
    """
    # Internal methods:

    # Private methods:

    # Public methods: